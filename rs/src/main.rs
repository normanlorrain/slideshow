//! magic-lantern — Rust port (work in progress).
//!
//! Phase 0 delivered spikes as examples:
//!   cargo run --example spike_exif
//!   cargo run --example spike_pdf
//!   cargo run --example spike_display
//!
//! Full CLI arrives in later phases; see rust.md.

fn main() {
    eprintln!(
        "magic-lantern Rust port (phase 0 scaffold).\n\
         \n\
         Run the Phase 0 spikes:\n\
           cargo run --example spike_exif\n\
           cargo run --example spike_pdf\n\
           cargo run --example spike_display\n\
         \n\
         See rust.md for the porting plan and crate decisions."
    );
}
